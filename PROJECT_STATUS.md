# A Thousand Stories — Status

- Phase: Phase 1 — Daily Story Pipeline
- Milestone: Angular scaffold + deploy live; daily story pipeline defined (see `docs/daily-story-workflow.md`)
- Content: 11 stories in `src/assets/data/stories.json`. New stories are detailed 600–1,000-word narratives with a ~60-word catchy hook, real sources, and a free-license hero image.
- Done this pass:
  1. Per-story SEO: `seo.service.ts` (title, meta description, canonical, Open Graph, Twitter cards, Article JSON-LD) wired into story-detail + static-page defaults on route change
  2. `sitemap.xml` + `robots.txt` generated by `scripts/generate-sitemap.mjs` (run in CI before build)
  3. Mobile: hamburger nav ≤760px; contrast fixes (`--muted #6f665b`, `--on-ink-muted`)
  4. COLOR.md rewritten to match the real warm editorial palette (was stale navy/pink)
- Next steps:
  1. Continue daily stories (12/1000 next)
  2. Optional later: @angular/ssr prerendering for strongest per-story indexability
  3. Visual QA on a real phone (header, portrait, filters)
- Blockers: None
- Note: `ng lint` has no target configured (ESLint not installed). Definition of Done lint step skipped until we add angular-eslint (ask first before adding deps).
- Last updated: 2026-09-07
